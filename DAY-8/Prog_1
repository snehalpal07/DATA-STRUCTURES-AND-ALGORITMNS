
#include <stdio.h>

long long power(long long a, int b) {
    if (b == 0)
        return 1;
    return a * power(a, b - 1);
}

int main() {
    printf("%lld\n", power(2, 3));  // 8
    printf("%lld\n", power(5, 0));  // 1
    return 0;
}
